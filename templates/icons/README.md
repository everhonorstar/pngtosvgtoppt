# Icons

This directory is intentionally empty in the lightweight `pngtosvgtoppt` skill.

For reconstructed PNG scenes, prefer direct SVG shapes or isolated image layers. If you need `<use data-icon="library/name">` placeholders, copy the required icon library folders here before running `scripts/finalize_svg.py`.
